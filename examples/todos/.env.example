# `npx tanstack-db-notion init` creates .env.local automatically.
# Use this file only for manual setup.
NOTION_PAT=

# Set either one. A database ID is accepted when it has exactly one data source.
NOTION_DATA_SOURCE_ID=your_data_source_id
# NOTION_DATABASE_ID=your_database_id
PORT=8787
